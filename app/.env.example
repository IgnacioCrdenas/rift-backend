# Example .env file for the application
# Replace placeholders with actual values before running the application

# Firebase Configuration
DATABASE_URL=https://your-database-url.firebaseio.com
FIREBASE_CREDENTIALS=path/to/serviceAccountKey.json
FIREBASE_STORAGE_BUCKET=your-storage-bucket.appspot.com

# Environment Configuration
ENVIRONMENT=development   # Possible values: development, production
DEBUG=True                # True for debugging, False in production

# Other Configuration (optional, based on your application needs)
# SECRET_KEY=your-secret-key
# API_VERSION=v1